
#   This is a comment
### This is another comment

#[
    Multi-line comments?
]##[
    Lets try to break the lexer.
]#

import imports
from hello  import world
from i_will import fubar as this_program

func main(): u32 {

    let foo: class = Foobar()
    println(foo.get_length(foo.txt))
    return 0
}

class Foobar {
    func init(self): void {
        self.txt = 'hello'
    }

    func get_length(txt: str): u32 {
        return txt.len()
    }
}
